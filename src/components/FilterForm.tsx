import React from "react";

type Props = {
  onSubmit: () => void;
};

const FilterForm = ({onSubmit}: Props) => {
  return (
    <div>
      <form action=""></form>
    </div>
  );
};

export default FilterForm;
